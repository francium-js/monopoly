import { ModalsEnum } from 'src/enums'
import TemplateModal from 'src/features/modals/template'

const modalsList = [{ component: TemplateModal, name: ModalsEnum.TEMPLATE }]

export default modalsList
